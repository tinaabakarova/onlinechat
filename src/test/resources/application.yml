spring:
  datasource:
    initialization-mode: always
    driver-class-name: com.mysql.cj.jdbc.Driver
    data: classpath:data.sql


  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    database: mysql

    show-sql: true

debug: true